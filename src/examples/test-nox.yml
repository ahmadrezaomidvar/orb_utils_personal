description: >
  You can use test-nox job to test using nox. It uses the conda environment file to create the environment and then runs nox.
usage:
  version: 2.1
  orbs:
    orb_utils_personal: ahmadrezaomidvar/orb_utils_personal@1.0.0
  workflows:
    test-nox:
      jobs:
        - orb_utils_personal/test-nox:
            context: nox
